name: start_activity
description: A new Flutter package.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intent: ^1.1.0
  android_intent: ^0.3.5+1
  get_version: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
